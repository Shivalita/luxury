{% extends 'base.html.twig' %}

{% block title %}Candidatures{% endblock %}

{% block body %}
    <h1>Candidatures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidature.id }}</td>
            </tr>
            <tr>
                <th>IdCandidate</th>
                <td>{{ candidature.idCandidate }}</td>
            </tr>
            <tr>
                <th>IdOffer</th>
                <td>{{ candidature.idOffer }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{{ candidature.datetime ? candidature.datetime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('candidatures_index') }}">back to list</a>

    <a href="{{ path('candidatures_edit', {'id': candidature.id}) }}">edit</a>

    {{ include('candidatures/_delete_form.html.twig') }}
{% endblock %}
